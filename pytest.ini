[pytest]
markers =
    smoke: Run after every new build - critical path only
    regression: Full suite - run for complete validation
    negative: Error handling and validation scenarios
    order: Test execution order
